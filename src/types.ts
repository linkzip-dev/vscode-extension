export type ProjectConfig = {
  projectId: string;
  buildDirectory: string;
};

export type LinkZipConfig = {
  "API-TOKEN": string;
};

export type ApiResponse = {
  status: string;
  message: string;
};
