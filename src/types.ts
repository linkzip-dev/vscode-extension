export type ProjectConfig = {
  project_id: string;
  build_dir: string;
};

export type LinkZipConfig = {
  "API-TOKEN": string;
};

export type ApiResponse = {
  status: string;
  message: string;
};
